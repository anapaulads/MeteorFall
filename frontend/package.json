{
  "name": "meteorfall-cesium",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 8080",
    "build": "vite build",
    "preview": "vite preview",
    "start:backend": "cd ../backend && py -m flask run",
    "start": "concurrently \"npm:start:backend\" \"npm:dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.3.1",
    "vite-plugin-cesium": "^1.2.22"
  },
  "dependencies": {
    "cesium": "^1.117.0"
  }
}